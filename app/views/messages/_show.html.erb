<div class="card-message-todo">
  <% if message.submit_message_date %>
    <div class="message-date">Note ajoutée le <%=l(message.submit_message_date, format: '%d %B %Y', :locale => 'fr') %></div>
  <% end %>
  <div class="message-content"> <%=message.note %> </div>
</div>
